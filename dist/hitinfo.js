export class HitInfo {
    constructor(distance, point, normal, color) {
        this.distance = distance;
        this.point = point;
        this.normal = normal;
        this.color = color;
    }
}
